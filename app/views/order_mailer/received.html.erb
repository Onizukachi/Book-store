<h1>Dear <%= @order.name %></h1>
<p>Thank you for your recent order from The Hikaru Book Store.</p>
<p>You ordered the following items:</p>
<table>
  <tr><th colspan= "2">Qty</th><th>Description</th></tr>
  <% @order.line_items.each do |line_item| %>
    <tr>
      <td class='text-right'>
        <%= line_item.quantity %>
      </td>
      <td>&times;</td>
      <td class='pr-2'><%= line_item.product.title %></td>
      <td class='font-bold text-right'>
        <%= number_to_currency(line_item.total_price) %>
      </td>
    </tr>
  <% end %>
</table>

<p>We'll send you a separate e-mail when your order ships.</p>